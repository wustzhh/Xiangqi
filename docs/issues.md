# 待修复问题清单

## 1. 手机连接不上服务器

**描述**：手机端无法连接到远程 VPS `212.129.243.158:8080`。电脑端可以正常连接，说明服务器运行正常，但手机网络（移动数据/WiFi）可能屏蔽了 8080 端口。

**可能原因**：
- 运营商封锁非标准端口
- VPS 防火墙未开放 8080
- 需要 WSS（WebSocket Secure）而非 WS

**解决方案**：
- 方案 A：在 VPS 上用 Nginx 反向代理，将 `wss://212.129.243.158/ws` 转发到 `ws://127.0.0.1:8080`，客户端改用 443 端口 + wss 协议
- 方案 B：服务器改用 443 或 80 端口（需管理员权限）
- 方案 C：客户端设置页面允许用户自定义服务器地址（已有该功能）

**状态**：❌ 未完成

## 2. 无将死/困毙判定

**描述**：游戏中没有将军、将死和困毙的自动判定。

**状态**：✅ 已修复
**修复方案**：客户端 room_screen 走棋后调用 _checkGameEnd() 检测将死/困毙，检测到后发送 game_over 到服务器广播。

## 3. 认输流程需验证

**描述**：认输后房间列表状态可能不对。

**状态**：✅ 已修复
**修复方案**：handleResign + _endGame + handleGameOver 完整覆盖三种结束方式，房间状态正确设置 finished。

## 4. 断线重连后棋盘状态未恢复

**描述**：重连后看到初始棋盘而不是实际对局位置。

**状态**：✅ 已修复
**修复方案**：Room.moveHistory 存储走法，tryReconnect 发送 history，客户端 _replayMoves 重放重建棋盘。

## 5. Windows 客户端文件被占用

**描述**：applications/windows-client/ 下的文件在运行时无法覆盖。

**解决方案**：复制前执行 taskkill /f /im xiangqi.exe。

**状态**：❌ 未完成

## 6. applications/xiangqi.apk 体积过大

**描述**：APK 52MB 接近 GitHub 50MB 限制。

**状态**：✅ 已修复
**修复方案**：.gitignore 忽略 applications/*.apk 等预构建产物。

## 7. 房间设置未保存到服务器

**描述**：房间设置（悔棋/步时/局时/先后手）只在内存中，服务器重启后丢失。计时器功能未实现。

**状态**：❌ 未完成

## 8. 无法开始对局

**描述**：
> 现在有界面了，而且界面不好看，特别丑,悔棋分好几种，无悔，双方同意才悔棋，强制悔棋(3次),而且我说的准备和开始按钮没看到,应该是顶层显示玩家头像和ID，然后显示等待玩家加入，然后中间区域显示设置信息，靠底下的位置显示准备和开始按钮

**状态**：✅ 已修复
**修复方案**：大厅 lobby_screen 把完整消息数据通过 constructor initialData 传给 RoomScreen，RoomScreen 改用 _initFromData() 从 initialData 恢复状态，避免消息被大厅消费的竞态问题。设置面板对所有玩家可见（非房主只读）。

**2026-07-14 二次美化**：
- 去掉红色调试条
- 重写等待界面布局：顶层头像卡片（CircleAvatar + 红/黑方标签 + "我" + "已准备"徽标）→ 金色等待状态提示条 → 设置面板（悔棋三种模式下拉 + 步时/局时 +/- 按钮 + 先后手下拉，白色卡片阴影风格）→ 底部固定操作栏
- 底部按钮先手/后手分列：
  - 先手（房主）：准备（左） + 开始（右）两个按钮并排
  - 后手（非房主）：准备按钮居中
- 使用中国象棋风格配色（暗红 #8B0000、金色 #FFD700 点缀）
- AppBar 添加象棋旗帜图标

## 9. 创建问题追踪 skill

**描述**：用户要求创建一个 track-issues skill，规范问题记录和修复流程。

**状态**：✅ 已修复
**修复方案**：创建 .reasonix/skills/track-issues/SKILL.md，包含问题记录、修复方案追加、新问题前确认上一个问题状态等规则。

## 10. 网络对战模式下面问题颇多
> 1. 房主创房之后再离开，房间没有消失 ✅
- a. 房主离开→转让给下一个玩家
- b. 对局中离开→30秒重连计时
- 房间是否解散应该是由服务器监听和决定的，而不是让客户端上传信号之后再让服务器解散房间，因为客户端可以直接杀进程
> 2. 开战动画不在屏幕中间/颠倒/不全屏/看不到
- a. 现在是有背景，但是没有对剑的动画
> 3. 对局中离开后没有重连选项
- 此处应该是没有检测ID导致的，房间对战双方的ID要由服务器记录，而不是让客户端记录，因为客户端杀进程之后就不在记录任何数据了
> 4. 棋子移动没有动画/只是平移
- a. 现在是延时瞬移
> 5. ~~观战有准备按钮~~ ✅ _isSpectator 隐藏操作栏


## 11. 网络对战模式功能
1. 悔棋、求和，认输
